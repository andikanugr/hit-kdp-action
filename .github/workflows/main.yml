on: [push]

jobs:
  testing-action:
    runs-on: ubuntu-latest
    name: Action Test
    steps:
      - name: Hit KDP
        id: KDP
        uses: andikanugr/hit-kdp-action@v1.0.6
        with:
          service: 'hit-kdp-action'
          repo: 'hit-kdp-action'
          org: 'andikanugr'
          token: ${{ secrets.GITHUB_TOKEN }}

