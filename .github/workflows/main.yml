on: [push]

jobs:
  testing-action:
    runs-on: ubuntu-latest
    name: Action Test
    steps:
      - name: Hit KDP
        id: KDP
        uses: andikanugr/hit-kdp-action@v1.0.2
        with:
          service: 'KDP'
          content: ${{ secrets.TOKEN }}

